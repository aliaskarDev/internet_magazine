import 'package:flutter/material.dart';

class CatologScreen extends StatelessWidget {
  const CatologScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}